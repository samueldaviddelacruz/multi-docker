sudo: required

services:
  - docker

before_install:
  - docker build -t samuel023/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run samuel023/react-test npm test -- --coverage

after_success:
  - docker build -t samuel023/multi-client ./client
  - docker build -t samuel023/multi-nginx ./nginx
  - docker build -t samuel023/multi-server ./server
  - docker build -t samuel023/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push samuel023/multi-client
  - docker push samuel023/multi-nginx
  - docker push samuel023/multi-server
  - docker push samuel023/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-west-2
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-west-2-523999790158
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_key_id:
    secure: $AWS_SECRET_KEY
